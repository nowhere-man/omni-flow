# OmniFlow icons

These bundled 24×24 stroke SVGs use the Lucide visual grammar and are the canonical assets for persisted `icon_key` values on every platform. Platform renderers must load these files by key instead of persisting SF Symbols or Material icon names.

Lucide project: https://lucide.dev/
