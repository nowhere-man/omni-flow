# OmniFlow Agent Rules

以下规则适用于整个仓库。

## 1. 模块边界

- `core` 是 Android library，负责业务规则、领域模型、持久化、格式化、校验、导入/导出、同步及其他 UI 无关行为。
- `app` 是 Android application，负责 Jetpack Compose UI、导航、生命周期、权限、通知、生物识别、文件选择和其他 Android 集成。
- `app` 复用 `core` 的模型、服务和规则，不重复实现业务逻辑。
- 修改 `core` 行为时，同一次变更中验证相关 core 测试、数据库迁移和 `app` 调用点。
- 新增 helper 或依赖前搜索现有实现；优先使用标准库、Android API 和已安装依赖。

## 2. 实现纪律

- 编辑前把请求转化为可观察的验收标准，并阅读相关实现、调用点及用户提供的截图。
- 修复负责问题的共享状态、样式、格式化器或服务，并检查同级调用点；避免逐页修补同一根因。
- 仅修改实现请求所必需的代码；不顺手重构、重命名、重新格式化或清理无关内容。
- 复用现有实现，保持最小 diff；不添加推测性功能、单次使用抽象或未经要求的可配置性。
- 先完成一批相关修改，再按风险运行验证；无需在每次小改动后编译。

## 3. Android UI

- 遵循 Material Design 3，优先使用原生 Compose/Material 组件、主题 token 和语义化颜色。
- 通过字体、尺寸、字重、间距、颜色和位置建立层级；相关控件应紧凑、对齐、清晰分组。
- 浅色和深色模式下，选中控件的文字与图标必须和背景保持足够对比度。
- 收入、支出、错误、破坏性操作和次要文本使用对应语义颜色；主题色不替代语义含义。
- 同一操作使用一致组件；避免文本裁切、无意义卡片、重复标题、过大筛选器和过多空白。
- 图标应匹配操作或领域，优先使用当前图标目录并遵循主题。
- 分类显示名使用 `一级分类 - 二级分类`；没有二级分类时省略分隔符。
- 日期文本使用产品指定格式、保持单行，并准确表示实际选中范围。

## 4. 验证与数据安全

- `core` 逻辑变更运行相关单元测试；数据库或 migration 变更同时运行 SQLDelight 迁移校验。
- Android 发布验证运行 core JVM 测试、SQLDelight 迁移校验、Android release lint 和要求的 release 构建。
- UI 或交互变更检查浅色/深色模式、主题色、选中/未选中、空/有数据/错误、导航、文本截断和小屏幕布局；仅检查源码不足以证明完成。
- 私有账单样本、凭据、签名材料和个人数据不得进入提交。依赖本地私有 fixture 的测试必须在 CI 中明确处理 fixture 缺失。

## 5. GitHub Issue、Git 与发布

- 解决 GitHub Issue 前获取完整的当前开放 Issue 列表，并阅读每个 Issue 的正文、评论、编辑记录和附件；最终审计前刷新该列表。
- 优先使用结构化 GitHub 连接器或已认证的 CLI/API。私有仓库通过未认证 API 返回 `404` 时，使用用户已登录的 Chrome 会话只读核实。
- 仅在用户明确要求时提交、推送、打标签或发布。
- 创建或移动标签前检查从上一个发布标签到目标提交的 commit diff；先推送 commit，再确认远端分支和解引用标签均指向预期提交。
- 发布说明必须基于实际标签 diff，使用简体中文并恰好包含以下两个面向产品的 section：

  - `## What's Changed` — 新增的用户可见能力和行为。
  - `## What's Updated` — 修复、优化、兼容性、UI 打磨和值得说明的内部改进。

- 持续观察所有必需 GitHub Actions job。失败时读取准确日志、修复根因、重新推送，并按需更新标签，直到全部成功。
- 发布完成后确认 GitHub Release 包含预期的 Android APK。

## 6. 完成标准

- “已实现”表示请求的行为存在于源码中；“已完成”要求所有相关源码、测试、构建、运行时/UI、CI、标签和发布资产均有权威证据。
- 交接前逐项审计明确要求；证据缺失、过窄或间接时继续验证，不宣称完成。
